# Local Server

The local development and runtime entry point for the Game Master Server
belongs here. It shares the same public repository as `host_server`.

Run `npm run dev` from this folder after installing `host_server` dependencies.
It invokes the Host Server locally; it does not create a second server product.
